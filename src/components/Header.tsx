import { Theme } from "./Theme"

export const Header = () => {
    return (
        <header className="header">
            <nav>
                <h2 className="header_title">Todos 2024 </h2>
                <Theme />
            </nav>
        </header>
    )
}
